import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dailygrats.app',
  appName: 'DailyGrats',
  webDir: 'dist/daily-grats/browser'
};

export default config;
